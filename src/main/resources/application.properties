server.port=8081
spring.application.name=hotels-service
HOSTNAME=${HOSTNAME:hotels-service}

#logging.config=classpath:logback-spring-local.xml

jwt.secret=devops_secret_key_2024

cors.allowed-origin=http://localhost:4200

spring.jpa.database=POSTGRESQL
spring.datasource.url=jdbc:postgresql://postgres-db-hotels:5432/devops_hotels_db
spring.datasource.username=postgres
spring.datasource.password=devops123

management.tracing.sampling.probability=1.0
management.zipkin.tracing.endpoint=http://zipkin:9411/api/v2/spans

spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

user.service=http://user-service:8080

logging.level.root=INFO
logging.level.org.springframework.security=DEBUG

#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

upload.dir=uploads